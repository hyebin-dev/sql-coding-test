-- 파일 상단 주석 템플릿
/*
[문제명] :
[레벨] :
[링크] :

[Key] : (핵심 개념 2~4개) 예) LEFT JOIN, IS NULL, GROUP BY, HAVING
[Notes] : (내가 조심할 실수 1~2개) 예) LEFT JOIN 후 WHERE 필터로 INNER처럼 변하는지 점검
[Tags] : (2~4개) 예) left-join, null, group-by
*/

-- SQL 작성 규칙(권장)
-- 1) SELECT 컬럼은 줄바꿈 가능
-- 2) JOIN은 JOIN마다 한 줄
-- 3) WHERE는 조건당 한 줄
-- 4) GROUP BY / ORDER BY도 항목당 한 줄
-- 5) NULL 비교는 IS NULL / IS NOT NULL

-- Write your query below